#pragma once
#include <bits/stdc++.h>

namespace Algorithms {
    bool divBy2(long long int n);
    bool divBy3(long long int n);
    bool divBy5(long long int n);
    bool divBy7(long long int n);
    bool divBy10(long long int n);
}
