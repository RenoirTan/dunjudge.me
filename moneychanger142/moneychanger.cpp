#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, v;
    cin >> n >> v;
    int coins[n];
    int* c = coins;
    for (int i = 0; i < n; i++) {
        cin >> *(c+i);
    };
    int coinSum = 0;
    int totalVal = 0;
    while (totalVal) {
        break;
    }
};