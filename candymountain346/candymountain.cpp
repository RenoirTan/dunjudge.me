#include <iostream>
using namespace std;

int main()
{
    int stones, maxStep;
    cin >> stones >> maxStep;
    int candies[stones];
    for (int i = 0; i < stones; i++) {
        cin >> candies[i];
    }
}
