#include <iostream>
#include <math.h>
using namespace std;

#define i64 long long

int main()
{
    // The only time watching ted ed riddles actually helped
    i64 lights;
    cin >> lights;
    cout << (i64)sqrt(lights);
    return 0;
}